<div class="dash-container">
    {{>navBar}}
    <main class="container" style="margin-top: 50px;">
        <div class="row">
            {{#each stories}}
                {{>storycard}}
            {{/each}}
        </div>
    </main>
    {{>footer}}    
</div>